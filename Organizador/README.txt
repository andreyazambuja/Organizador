Es programa gera (caso não existam) as pastas "Documentos" e "Planilhas", organizando os arquivos ".doc", ".docx" e ".xls" nas pastas correspondentes. Seguem orientações para utilização do programa:

1. Salvar o diretório "Organizador" na pasta onde você pretende organizar os arquivos.
2. Executar o programa.